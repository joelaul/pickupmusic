// For https://app.pickupmusic.com/settings/account

// The above page links to https://pickup.memberful.com/auth/sign_in for legacy user login. We should be able to consolidate leg and non-leg login to one portal, and generate appropriate components dynamically, so I omitted it from the routing.

export default function Account() {
  return <div>Account</div>;
}
