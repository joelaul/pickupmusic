// For https://app.pickupmusic.com/artists

// Artist entries link to a /search page with filtered results (e.g. ariel posen's "page" is the search page with "ariel posen" as filter). Would it be more performant to give each artist their own page?

export default function Artists() {
  return <div>Artists</div>;
}
